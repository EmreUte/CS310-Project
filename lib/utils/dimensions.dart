import 'package:flutter/material.dart';

class Dimen {
  static get cardMargins => const EdgeInsets.fromLTRB(0, 8, 0, 8);
  static get screenPadding => const EdgeInsets.fromLTRB(30, 60, 30, 80);
  static get textboxPadding => const EdgeInsets.symmetric(horizontal: 16, vertical: 12);
  static get buttonPadding => const EdgeInsets.symmetric(vertical: 14);
}